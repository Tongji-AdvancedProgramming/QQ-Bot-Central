dubbo:
  application:
    name: qq-bot-application
    logger: slf4j
  protocol:
    name: tri
    port: -1
  registry:
    address: zookeeper://${zookeeper.address:127.0.0.1}:2181
    check: false
server:
  port: 9900
zookeeper:
  address: zookeeper
